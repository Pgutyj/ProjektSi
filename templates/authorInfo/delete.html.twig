{% include 'default/_form.html.twig' with {
    page_title: 'title.author_info_delete'|trans({'%id%': authorInfo.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'author_info_index',
} %}
