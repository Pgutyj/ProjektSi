{% include 'default/_form.html.twig' with {
    page_title: 'title.author_info_edit'|trans({'%id%': authorInfo.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'author_info_index',
} %}
