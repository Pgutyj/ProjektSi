{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.author_list'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.author_list'|trans }}</h1>
    {% if pagination is defined and pagination.items|length %}

        <div class="table-responsive">
        <table class="table table-striped">
            <thead>
            <tr>
                <th>{{ knp_pagination_sortable(pagination, 'label.id'|trans, 'authorInfo.id')  }}</th>
                <th>{{ knp_pagination_sortable(pagination,'label.title'|trans, 'authorInfo.name') }}</th>
                <th>{{ 'label.create'|trans }}</th>
                <th>{{ 'label.actions'|trans }}</th>
                <th>{{ 'label.editing'|trans }}</th>
                <th>{{ 'label.delete'|trans  }}</th>
            </tr>
            </thead>
            <tbody>
            {% for authorInfo in pagination.items%}
                <tr>
                    <td>{{ authorInfo.id }}</td>
                    <td>{{ authorInfo.name }}</td>
                    <td>
                        <a class="btn btn-outline-info" href="{{ url('author_info_create') }}" title="{{ 'action.create'|trans }}">
                            {{ 'action.create'|trans }}
                        </a>
                    </td>
                    <td>
                        <a href="{{ url('author_info_show', {slug: authorInfo.slug}) }}" title="{{ 'action.view'|trans }}" class="btn btn-outline-primary">
                            {{ 'action.view'|trans }}
                        </a>
                    </td>
                    <td>
                        <a href="{{ url('author_info_edit', {slug: authorInfo.slug}) }}" title="{{ 'action.edit'|trans }}" class="btn btn-outline-success">
                            {{ 'action.edit'|trans }}
                        </a>
                    </td>
                    <td>
                        <a href="{{ url('author_info_delete', {slug: authorInfo.slug}) }}" title="{{ 'action.delete'|trans }}" class="btn btn-outline-danger">
                            {{ 'action.delete'|trans }}
                        </a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        </div>
        <div class="navigation text-center">
            {{ knp_pagination_render(pagination) }}
        </div>

    {% else %}
        <p>
            {{ 'message.empty_list'|trans }}
        </p>
    {% endif %}
{% endblock %}