{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.profile_edit'|trans({'%id%': app.user.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.profile_edit'|trans({'%id%': app.user.id|default('')}) }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action.save'|trans }}" class="btn btn-primary"/>
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('app_profile') }}" class="btn btn-link">
            {{ 'action.back_to_list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}