{% include 'default/_form.html.twig' with {
    page_title: 'title.profile_edit'|trans({'%id%': app.user.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'app_profile',
} %}
