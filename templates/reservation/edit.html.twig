{% include 'default/_form.html.twig' with {
    page_title: 'title.reservation_edit'|trans({'%id%': reservation.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'your_reservations',
} %}